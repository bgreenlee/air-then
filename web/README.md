# Web application

The Next.js frontend is currently mounted at the repository root (`app/`) to work with the bundled Sites runtime. This directory is reserved as the standalone web package boundary when deploying the frontend separately from the API; its runtime source is intentionally kept at root for the local build command.
